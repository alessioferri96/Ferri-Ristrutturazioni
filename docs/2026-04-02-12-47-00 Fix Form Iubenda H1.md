# Fix Form, Iubenda e Heading H1

**Data:** 2026-04-02
**Severita:** CRITICAL

## Descrizione

Correzione di problemi critici su form, cookie banner e struttura heading.

## Modifiche Effettuate

### Fix 1: Attributi `name` e `required` sui form (5 pagine)
Tutti gli `<input>` e `<textarea>` nei form di contatto avevano solo `id` ma non `name`. Con `mailto:` i dati non venivano inclusi nell'email.
- Aggiunto `name` corrispondente all'id
- Aggiunto `required` su nome, email, messaggio e checkbox privacy

### Fix 2: Iubenda Cookie Widget
- Aggiunto `async` allo script su index.html, chi-siamo.html, contatti.html
- Aggiunto lo script mancante su servizi.html e progetti.html (con `defer`)
- Nota: successivamente `async` rimosso e sostituito con caricamento sincrono/defer per compatibilita con Iubenda

### Fix 3: Doppio H1 su progetti.html
- Secondo `<h1>` (slide 2) cambiato in `<h2>` mantenendo le stesse classi

## File Modificati
- `index.html`, `chi-siamo.html`, `servizi.html`, `progetti.html`, `contatti.html`

## Riepilogo Post-Implementazione
Completato. Form funzionanti, cookie banner visibile su tutte le pagine, un solo H1 per pagina.
